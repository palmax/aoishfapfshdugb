CREATE TABLE PARTO (n_crotal          varchar(32),
                    fecha_prev_parto  date,
                    fecha_min_parto   date,
                    fecha_max_parto   date,
                    PRIMARY KEY (n_crotal));